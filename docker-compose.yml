services:
  frontend:
    container_name: llquiz-frontend
    image: llquiz-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
